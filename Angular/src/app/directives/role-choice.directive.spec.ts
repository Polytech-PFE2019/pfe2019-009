import { RoleChoiceDirective } from './role-choice.directive';

describe('RoleChoiceDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleChoiceDirective();
    expect(directive).toBeTruthy();
  });
});
